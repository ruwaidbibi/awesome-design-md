{
  "name": "us-eastern-churches",
  "private": true,
  "version": "0.1.0",
  "description": "Directory of Eastern Catholic, Eastern Orthodox, Oriental Orthodox and Church of the East parishes in the United States, with their social accounts and events.",
  "workspaces": ["pipeline", "web"],
  "scripts": {
    "pipeline": "npm run -w @churches/pipeline start --",
    "harvest": "npm run -w @churches/pipeline start -- harvest",
    "enrich": "npm run -w @churches/pipeline start -- enrich",
    "events": "npm run -w @churches/pipeline start -- events",
    "build:data": "npm run -w @churches/pipeline start -- build",
    "refresh": "npm run -w @churches/pipeline start -- refresh",
    "dev": "npm run -w web dev",
    "build": "npm run -w web build",
    "typecheck": "npm run -w @churches/pipeline typecheck && npm run -w web typecheck"
  },
  "engines": { "node": ">=20.11" }
}
